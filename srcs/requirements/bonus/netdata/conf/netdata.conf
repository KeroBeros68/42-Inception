# Minimal override — every key left unset falls back to netdata's
# compiled-in defaults (proc/cgroups/network plugins stay enabled).

[global]
    hostname = inception-netdata
    process scheduling policy = idle

[web]
    bind to = *
    default port = __NETDATA_PORT__
